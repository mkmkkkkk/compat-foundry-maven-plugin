# 0.2.0 release receipt

- Intended tag: `v0.2.0`
- Intended repository: `https://github.com/mkmkkkkk/compat-foundry-maven-plugin`
- Local release subtree: `release/compat-foundry-maven-plugin/`
- Public push: **blocked by the task's later hard constraint forbidding push**.
- JitPack build/API verification: **blocked** (push is forbidden; network/JitPack verification is therefore not run).
- Maven verification: **blocked**; `mvn` is not installed or available on PATH in this environment.
- Private-material grep for `michaelyang`, `macmini`, `mkyang.ai`, `Resend`, `outreach`: empty.
- Private filenames excluded: `decision.md`, demand/outreach/research/report/evidence/lab/fixture/deploy files,
  web service prototype, and `BEHAVIOR_CONTRACT.md`.

The included example is at `examples/boot27/`; its expected seven-finding verification remains unverified here.
